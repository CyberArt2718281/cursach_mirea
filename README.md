# Платформа для управления событиями и мероприятиями

Полнофункциональная платформа для управления событиями с онлайн-регистрацией.

## Технологический стек

### Backend

- **Node.js** + **Express.js**
- **MongoDB 7+** с Mongoose
- JWT аутентификация
- Express Validator для валидации
- CORS, Morgan для логирования

### Frontend

- **Angular 14**
- Reactive Forms
- HttpClient для работы с API
- Роутинг с guards
- HTTP Interceptors

## Структура проекта

```
курсовая_работа/
├── backend/          # Backend на Express
│   ├── models/      # Mongoose модели
│   ├── routes/      # API маршруты
│   ├── middleware/  # Middleware функции
│   ├── server.js    # Точка входа
│   └── .env         # Конфигурация
├── frontend/        # Frontend на Angular
│   ├── src/
│   │   ├── app/
│   │   │   ├── components/  # Компоненты
│   │   │   ├── services/    # Сервисы
│   │   │   ├── models/      # TypeScript модели
│   │   │   └── guards/      # Route guards
│   │   └── environments/    # Конфигурация окружений
│   └── angular.json
└── README.md
```

## Установка и запуск

### Требования

- Node.js 16+
- MongoDB 7+
- npm или yarn

### Backend

1. Перейдите в папку backend:

```bash
cd backend
```

2. Установите зависимости:

```bash
npm install
```

3. Настройте `.env` файл:

```env
PORT=5000
MONGODB_URI=mongodb://localhost:27017/events_management
JWT_SECRET=your_secret_key
NODE_ENV=development
```

4. Запустите MongoDB (если не запущена)

5. Запустите сервер:

```bash
# Режим разработки
npm run dev

# Продакшн
npm start
```

Backend будет доступен на `http://localhost:5000`

### Frontend

1. Перейдите в папку frontend:

```bash
cd frontend
```

2. Установите зависимости:

```bash
npm install
```

3. Запустите приложение:

```bash
npm start
# или
ng serve
```

Frontend будет доступен на `http://localhost:4200`

## Функциональность

### Для всех пользователей

- ✅ Просмотр списка событий
- ✅ Фильтрация по категориям и статусу
- ✅ Поиск событий
- ✅ Просмотр деталей события
- ✅ Онлайн-регистрация на событие
- ✅ Получение номера регистрации

### Для администраторов

- ✅ Аутентификация с JWT
- ✅ Создание событий
- ✅ Редактирование событий
- ✅ Удаление событий
- ✅ Просмотр статистики по событиям
- ✅ Управление регистрациями
- ✅ Отметка посещения участников
- ✅ Панель администратора с общей статистикой

## API Endpoints

### События

- `GET /api/events` - Получить все события (с фильтрами и пагинацией)
- `GET /api/events/:id` - Получить событие по ID
- `POST /api/events` - Создать событие
- `PUT /api/events/:id` - Обновить событие
- `DELETE /api/events/:id` - Удалить событие
- `GET /api/events/:id/stats` - Статистика по событию

### Регистрации

- `GET /api/registrations` - Получить все регистрации
- `GET /api/registrations/:id` - Получить регистрацию по ID
- `GET /api/registrations/number/:number` - Получить по номеру
- `POST /api/registrations` - Создать регистрацию
- `PUT /api/registrations/:id` - Обновить регистрацию
- `PATCH /api/registrations/:id/attend` - Отметить посещение
- `DELETE /api/registrations/:id` - Отменить регистрацию

### Пользователи

- `POST /api/users/register` - Регистрация пользователя
- `POST /api/users/login` - Вход в систему
- `GET /api/users/profile` - Получить профиль (требуется токен)
- `PUT /api/users/profile` - Обновить профиль (требуется токен)
- `GET /api/users` - Все пользователи (только admin)

## Модели данных

### Event (Событие)

```typescript
{
  title: string;
  description: string;
  date: Date;
  endDate?: Date;
  location: string;
  category: string;
  capacity: number;
  availableSeats: number;
  price: number;
  imageUrl?: string;
  status: 'активное' | 'отменено' | 'завершено' | 'черновик';
  organizer: {
    name: string;
    email: string;
    phone?: string;
  };
  registrationDeadline?: Date;
  tags?: string[];
}
```

### Registration (Регистрация)

```typescript
{
  event: ObjectId;
  participant: {
    firstName: string;
    lastName: string;
    email: string;
    phone: string;
    organization?: string;
    position?: string;
  };
  status: 'подтверждена' | 'ожидание' | 'отменена';
  registrationNumber: string;
  paymentStatus: 'оплачено' | 'не оплачено' | 'возвращено';
  notes?: string;
  attended: boolean;
  attendedAt?: Date;
}
```

## Создание администратора

Для доступа к админ-панели создайте администратора через API:

```bash
curl -X POST http://localhost:5000/api/users/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "email": "admin@example.com",
    "password": "admin123",
    "role": "admin"
  }'
```

Или используйте любой API клиент (Postman, Insomnia и т.д.)

## Безопасность

- Пароли хешируются с использованием bcrypt
- JWT токены для аутентификации
- Protected routes с AuthGuard
- Валидация данных на backend
- CORS настроен для безопасности

## Дополнительные возможности

- 📊 Статистика по событиям
- 📧 Уникальные номера регистраций
- 🔍 Полнотекстовый поиск
- 📱 Адаптивный дизайн
- ⚡ Пагинация для больших списков
- 🎨 Современный UI/UX
- ✅ Валидация форм
- 🔒 Защищенные маршруты

## Разработка

### Backend

```bash
cd backend
npm run dev  # Запуск с nodemon
```

### Frontend

```bash
cd frontend
ng serve  # Запуск dev сервера
ng build  # Сборка для продакшн
```

## Лицензия

MIT

## Автор

Курсовая работа по разработке платформы для управления событиями
