# Настройка Email уведомлений

## Реализовано

### Backend

1. ✅ **Отправка email при регистрации пользователя**
   - Письмо с подтверждением email
   - Ссылка действительна 24 часа
2. ✅ **Отправка email при регистрации на событие**

   - Подтверждение регистрации
   - Номер регистрации
   - Детали события

3. ✅ **Новые API endpoints**
   - `GET /api/users/confirm-email/:token` - подтверждение email
   - `POST /api/users/resend-confirmation` - повторная отправка письма

### Frontend

1. ✅ **Страница подтверждения email** (`/confirm-email`)
2. ✅ **Красивый дизайн писем** с градиентами и современным стилем

## Настройка для Gmail

### 1. Включите двухфакторную аутентификацию

1. Перейдите в настройки Google аккаунта
2. Безопасность → Двухэтапная аутентификация
3. Включите её

### 2. Создайте пароль приложения

1. Откройте https://myaccount.google.com/apppasswords
2. В поле "Введите название приложения" напишите: **Events Platform** (или любое другое название)
3. Нажмите кнопку "Создать"
4. Скопируйте сгенерированный пароль (16 символов без пробелов)
5. **Важно:** Сохраните этот пароль - он больше не будет показан!

### 3. Настройте .env файл

Откройте `backend/.env` и добавьте:

```env
# Email настройки
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=ваш-email@gmail.com
EMAIL_PASSWORD=ваш-пароль-приложения
EMAIL_FROM=Платформа Событий <ваш-email@gmail.com>
FRONTEND_URL=http://localhost:4200
```

**Важно:**

- `EMAIL_USER` - ваш настоящий Gmail адрес
- `EMAIL_PASSWORD` - пароль приложения из шага 2 (не ваш обычный пароль!)
- `EMAIL_FROM` - может быть любое имя, но email должен совпадать с EMAIL_USER

## Альтернативные провайдеры

### Mail.ru

```env
EMAIL_HOST=smtp.mail.ru
EMAIL_PORT=465
EMAIL_USER=ваш-email@mail.ru
EMAIL_PASSWORD=ваш-пароль
```

### Yandex

```env
EMAIL_HOST=smtp.yandex.ru
EMAIL_PORT=465
EMAIL_USER=ваш-email@yandex.ru
EMAIL_PASSWORD=ваш-пароль
```

### Outlook/Hotmail

```env
EMAIL_HOST=smtp-mail.outlook.com
EMAIL_PORT=587
EMAIL_USER=ваш-email@outlook.com
EMAIL_PASSWORD=ваш-пароль
```

## Тестирование

### 1. Запустите backend

```bash
cd backend
npm run dev
```

При успешной настройке увидите:

```
✅ Email сервер готов к отправке писем
```

### 2. Зарегистрируйте нового пользователя

1. Откройте http://localhost:4200
2. Зарегистрируйтесь с реальным email
3. Проверьте почту - должно прийти письмо с подтверждением

### 3. Зарегистрируйтесь на событие

1. Выберите любое событие
2. Зарегистрируйтесь на него
3. Проверьте почту - придет подтверждение с номером регистрации

## Структура писем

### Письмо регистрации

- 🎉 Красивый header с градиентом
- Персональное приветствие
- Кнопка подтверждения email
- Срок действия ссылки (24 часа)

### Письмо регистрации на событие

- ✅ Подтверждение регистрации
- 🎫 Номер регистрации (крупным шрифтом)
- 📅 Детали события (название, дата, место, категория)
- Инструкции для посещения

## Troubleshooting

### Ошибка: "Invalid login"

- Проверьте правильность EMAIL_USER и EMAIL_PASSWORD
- Убедитесь, что используете пароль приложения, а не обычный пароль

### Ошибка: "Connection timeout"

- Проверьте EMAIL_HOST и EMAIL_PORT
- Убедитесь, что нет блокировки файрволом

### Письма не приходят

- Проверьте папку "Спам"
- Убедитесь, что EMAIL_FROM содержит реальный email
- Проверьте логи backend - должно быть "✅ Email отправлен"

### Письма приходят в спам

- Используйте настоящий домен (в production)
- Настройте SPF и DKIM записи
- Не отправляйте много писем за короткое время

## Production настройки

Для production рекомендуется использовать:

- **SendGrid** (бесплатно до 100 писем/день)
- **Amazon SES** (дешево и надежно)
- **Mailgun** (бесплатно до 5000 писем/месяц)

Настройка аналогична, просто замените SMTP параметры в .env
